{{#if skills}}
    <div class="section">
        <h3>Skills</h3>
        <ul class="skills">
            {{#each skills}}
                <li>{{this}}</li>
            {{/each}}
        </ul>
    </div>
{{/if}}