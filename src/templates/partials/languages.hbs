{{#if languages}}
    <div class="section">
        <h2>Languages</h2>
        <ul class="item-list">
            {{#each languages}}
            <li>
                {{language}} ({{proficiency}})
            </li>
            {{/each}}
        </ul>
    </div>
{{/if}}